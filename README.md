# SpeakEasy

Offline voice practice app (iOS / Android) built with Capacitor 6 + React + TypeScript.

- iOS build: see IOS_README.md
- Source layout: src/ (React app), ios/ (Capacitor iOS native plugins), public/tts (offline audio)
